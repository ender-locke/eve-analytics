

class Match:

    def __init__(self, match_id, ship_pilot_mapping):
        self.match_id = match_id
        # todo lets make the match data flow, and then fill in all
        #  the other classes each match has n Pilots and each Pilot has
        #  1 ship and each ship has its data
